{
  "name": "dungeons-of-caraross",
  "private": true,
  "version": "0.5.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "build:faces": "node tools/build-faces.ts",
    "build:levels": "node tools/gen-levels.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "sharp": "^0.34.1",
    "typescript": "^5.7.2",
    "vite": "^6.0.7",
    "vitest": "^3.0.4"
  }
}
